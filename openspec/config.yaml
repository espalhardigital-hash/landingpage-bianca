schema: spec-driven

context: |
  Tech stack: React + Vite (frontend), FastAPI Python (backend), PostgreSQL, MinIO, Tailwind CSS
  Styling: Premium Dark Glassmorphism (lavanda/menta glows, Space Grotesk + Inter)
  Domain: Landing page psicología juvenil - Lead capture (eBook + desafío 7 días)
  Language: Spanish (es)
  Infra: Docker + Docker Compose (local dev), Traefik externo en VPS producción
  Email: Resend API + Jinja2 HTML templates

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
